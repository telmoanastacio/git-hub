<thlogic>
    <attr sel="#headId"
         th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#result-message"
          th:text="${__${T(com.tsilva.util.AttributeNames).RESULT_MESSAGE}__}"/>
    <attr sel="#restart"
          th:href="@{__${T(com.tsilva.util.GameMappings).RESTART}__}"/>
    <attr sel="#home"
          th:href="@{__${T(com.tsilva.util.GameMappings).HOME}__}"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footer}"/>
</thlogic>